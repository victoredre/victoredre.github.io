---
// components imports
import {Icon} from "astro-icon/components";

// config import
import {SOCIALS} from '@/config';

---

<div class={'flex gap-380 md:gap-210'}>
    {
        SOCIALS.map((networkS) => (
            networkS.show && (
                    <a
                            class={`${networkS.name} text-x3 md:text-4xl text-silver-n200 hover:text-silver-200 rounded-md p-2 duration-100 hover:bg-neutral-200 dark:hover:bg-n700/40`}
                            href={networkS.url}
                            target="_blank"
                            aria-label={networkS.name + "-link"}
                    >
                        <Icon name={networkS.icon} class="text-n900 dark:text-n200"/>
                    </a>
            )
        ))
    }
</div>

